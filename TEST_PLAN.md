# CreateFluid × CreateBaton 全功能测试方案

> 测试范围：所有从 CreateBaton v0.5.0 移植到 CreateFluid 的新增功能

---

## 前置条件

| 项目 | 要求 |
|---|---|
| 游戏版本 | NeoForge 1.21.1，安装 Create 6.0.x |
| 模组 | CreateFluid 最新构建 |
| 语言设置 | 分别测试 `简体中文(zh_cn)` 和 `English(en_us)` |
| 必要物品 | 指挥棒、剪贴板、工程师护目镜 |
| 测试环境 | 创造模式，超平坦世界，准备充足的动力源 |

---

## 一、打包机 / 理包机 快捷操作

### 1.1 右键切换红石状态（打包机）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置一个打包机，手持指挥棒右键 | 屏幕下方显示 `"打包机已启动"` 或 `"打包机已关闭"`（青色文字），播放杠杆音效（pitch 1.5） |
| 2 | 再次右键同一个打包机 | 状态切换，文字提示对应变化 |
| 3 | 将语言切换为英文重复测试 | 显示 `"Packager powered on"` / `"Packager powered off"` |

### 1.2 右键切换红石状态（理包机）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置一个理包机，手持指挥棒右键 | 显示 `"理包机已启动/已关闭"` / `"Repackager powered on/off"` |

### 1.3 Shift+右键清除地址

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置打包机，用告示牌或剪贴板设置一个地址 | 地址设置成功 |
| 2 | 手持指挥棒 Shift+右键该打包机 | 显示 `"地址已清除"` / `"Address cleared"`，地址被清空 |
| 3 | 再次 Shift+右键（地址已为空） | 显示灰色提示 `"地址已为空"` / `"Address is already empty"` |
| 4 | 用告示牌设置地址后 Shift+右键 | 显示红色提示 `"告示牌地址无法清除"` / `"Sign address cannot be cleared"` |

---

## 二、工程师护目镜信息增强

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置打包机，不设置地址，戴上工程师护目镜查看 | 护目镜面板显示 `"打包机"`、 `"无地址"` |
| 2 | 用剪贴板设置地址 `#TestAddress`，戴上工程师护目镜查看 | 显示 `"打包机"`、 `"地址：TestAddress"` |
| 3 | 对理包机重复上述测试 | 标题显示 `"理包机"` / `"Repackager"` |

---

## 三、剪贴板地址粘贴

### 3.1 基本粘贴

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 手持剪贴板，写下 `#WarehouseA`（必须 `#` 开头） | 剪贴板内容有效 |
| 2 | 右键打包机 | 屏幕显示 `"打包机的地址已设置为 WarehouseA"` / `"Packager address set to WarehouseA"`，出现白色粒子效果 |
| 3 | 用工程师护目镜确认地址 | 地址显示为 `WarehouseA` |

### 3.2 无效地址

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 手持空剪贴板或写下不含 `#` 的内容 | — |
| 2 | 右键打包机 | 显示红色提示 `"剪贴板中没有有效的地址（请以#开头）"` / `"No valid address on clipboard (start with #)"` |

### 3.3 告示牌地址保护

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 用告示牌设置打包机地址 | 地址设置成功 |
| 2 | 手持写有 `#NewAddr` 的剪贴板右键该打包机 | 显示红色提示 `"打包机的地址由告示牌设置，无法覆盖"` / `"Packager address is set by sign, cannot override"`，地址不变 |

---

## 四、货物蛙港 + 锁链传动轮 配置

### 5.1 基本连接流程

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置货物蛙港和锁链传动轮，确保两者在 32 格范围内 | — |
| 2 | 手持指挥棒右键货物蛙港 | 显示金色提示 `"已选择青蛙港口"` / `"Frogport selected"`，播放音效，出现白色粒子螺旋，货物蛙港显示橙色轮廓 |
| 3 | 准星对准锁链传动轮的链条段 | 准星处显示白色小方块（选中点），链条显示青色轮廓线，货物蛙港到选中点之间有蓝色粒子流动 |
| 4 | 观察屏幕下方状态栏 | 显示 `"可以连接"` / `"Can connect"`（蓝色，每 5 tick 刷新） |
| 5 | 按右键（InteractionKeyMappingTriggered） | 显示 `"青蛙港口已连接至链条"` / `"Frogport connected to chain"`，播放风铃音效，退出货物蛙港模式 |
| 6 | 观察货物蛙港 | 它现在可以向锁链传动轮发送/接收包裹 |

### 5.2 距离过远

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 选中货物蛙港，准星对准超过 32 格的锁链传动轮 | 粒子连线变为红色，状态栏显示 `"距离太远，无法连接"` / `"Too far to connect"`（红色） |
| 2 | 按右键 | 显示红色提示 `"距离太远，无法连接"`，**不发送网络包** |

### 5.3 取消选择

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 选中货物蛙港后，再次右键同一个货物蛙港 | 显示灰色提示 `"已取消选择青蛙港口"` / `"Frogport deselected"`，退出模式，轮廓消失 |

### 5.4 无效链条 / 未对准

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 选中货物蛙港，准星对准空气或非链条方块 | 状态栏消失，无白色选中点 |
| 2 | 按右键 | 显示灰色提示 `"青蛙港口配置完成"` / `"Frogport configuration finished"`，退出模式 |

### 5.5 已连接货物蛙港预览（非编辑模式）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 将货物蛙港成功连接至锁链传动轮 | — |
| 2 | 手持指挥棒，准星对准该货物蛙港（不在任何编辑模式） | 显示货物蛙港到链条目标的蓝色粒子连线 |

---

## 五、邮箱 ↔ 列车站点 连接

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置邮箱和列车站点，确保在合理距离内 | — |
| 2 | 手持指挥棒右键邮箱 | 显示金色提示 `"已选择邮箱"` / `"Mailbox selected"`，邮箱显示橙色轮廓 |
| 3 | 右键列车站点 | 显示绿色提示 `"邮箱已连接至车站"` / `"Mailbox connected to station"`，退出邮箱模式 |
| 4 | 再次右键已连接的邮箱，然后右键同一个列车站点 | 显示红色提示 `"此邮箱已连接至该车站"` / `"This mailbox is already connected to this station"` |
| 5 | 选中邮箱后右键远处的列车站点（超出范围） | 显示红色提示 `"距离太远，无法连接"` / `"Too far to connect"` |

### 5.2 左键断开已连接站点

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 将邮箱连接至列车站点 | 连接成功 |
| 2 | 手持指挥棒右键该邮箱，重新进入邮箱模式 | 显示 `"已选择邮箱"`，邮箱显示橙色轮廓 |
| 3 | **左键**点击已连接的列车站点 | 显示红色提示 `"邮箱已与列车站点断开连接"` / `"Mailbox disconnected from station"`，播放物品拾取音效，退出邮箱模式 |
| 4 | 观察邮箱 | 不再向该列车站点发送/接收包裹 |

---

## 六、离心泵配置（原有功能修复）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 放置一个未封装的离心泵 | — |
| 2 | 手持指挥棒右键离心泵 | 屏幕显示 `"泵入"` / `"Pump in"` 或 `"泵出"` / `"Pump out"`（青色文字），播放杠杆音效（pitch 1.5），出现青色螺旋上升粒子 |
| 3 | 再次右键 | 模式切换，文字提示对应变化，粒子再次出现 |
| 4 | 用铜机壳封装离心泵 | — |
| 5 | 手持指挥棒右键已封装的离心泵 | **无任何反应**（封装后不再响应指挥棒，这是预期行为） |
| 6 | 分别测试中英文 | 两种语言下文字提示均正确显示 |

---

## 七、模式切换与取消（全局交互）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 进入任意编辑模式（货物蛙港 / 邮箱） | — |
| 2 | 手持指挥棒右键空气（RightClickEmpty 事件） | 退出当前模式，所有高亮/轮廓消失 |
| 3 | 进入任意编辑模式后，切换手持物品为非指挥棒 | 自动退出模式 |
| 4 | 进入动力臂 / 移液器 / 弹射置物台 选择模式后，右键货物蛙港 / 邮箱 等 | 先取消原有选择模式，再进入新的编辑模式 |

---

## 八、边界与异常测试

| 测试项 | 操作 | 预期结果 |
|---|---|---|
| 跨维度 | 在下界/末地中测试货物蛙港 / 邮箱连接 | 应拒绝连接或提示无效 |
| 权限不足 | 在服务器中由无建筑权限的玩家操作 | 货物蛙港/邮箱 操作被拒绝 |
| 方块已破坏 | 进入编辑模式后破坏选中的方块 | 模式自动退出，无崩溃 |
| 快速连续点击 | 快速连续右键打包机 / 货物蛙港 | 无崩溃，状态一致 |
| 超距连接 | 货物蛙港与锁链传动轮相距 >64 格 | 客户端预拒绝，服务器二次验证拒绝 |

---

## 测试通过标准

- [ ] 所有功能在 **中文** 环境下文字提示正确显示，无缺失键（无紫色 `create_fluid.xxx` 未翻译文本）
- [ ] 所有功能在 **英文** 环境下文字提示正确显示
- [ ] 所有网络操作在服务端有正确验证，无权限/越界漏洞
- [ ] 所有粒子效果和音效正常播放
- [ ] 所有轮廓/高亮渲染正常，无视觉残留（退出模式后必须清理干净）
- [ ] 无崩溃、无异常日志（检查 `.minecraft/logs/latest.log`）

---

## 快速回归测试清单（每次构建后必测）

```
□ 指挥棒右键打包机 → 文字提示 + 状态切换
□ 指挥棒右键货物蛙港 → 橙色轮廓 + 进入模式
□ 准星对准锁链传动轮 → 白色选中点 + 蓝色粒子连线
□ 右键连接货物蛙港 → 风铃音效 + 退出模式
□ 指挥棒右键离心泵 → 青色文字 + 螺旋粒子
□ 工程师护目镜看打包机 → 地址显示
□ 剪贴板右键打包机 → 地址设置 + 白色粒子

```
